# Nombre de la aplicación Spring Boot
spring.application.name=lol-builds

# Ruta al archivo JSON de configuración de Firebase
FIREBASE.CONFIG.FILE.PATH=src/main/resources/tu-archivo-firebase-config.json